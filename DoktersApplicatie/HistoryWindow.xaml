<Window x:Class="DoktersApplicatie.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DoktersApplicatie"
        mc:Ignorable="d"
        Title="HistoryWindow" Height="450" Width="800">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Session Time: " Margin="5,0,0,0"/>
            <TextBlock Text="{Binding SessionTime}" Margin="5,0,0,0"/>
            <TextBlock Text="Distance: " Margin="5,0,0,0"/>
            <TextBlock Text="{Binding Distance}" Margin="5,0,0,0"/>
            <TextBlock Text="Total Wattage: " Margin="5,0,0,0"/>
            <TextBlock Text="{Binding AccWatt}" Margin="5,0,0,0"/>
        </StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
        </Grid>
    </StackPanel>

</Window>
